🔑 ثانياً: نظام البصمة (WebAuthn / Passkey)
ما هو WebAuthn؟
معيار عالمي (W3C) يسمح للجهاز بإنشاء زوج مفاتيح تشفير (عام + خاص). المفتاح الخاص لا يغادر الجهاز أبداً.

مراحل التسجيل (مرة واحدة)
المستخدم يضغط "أضف بصمة"
         ↓
[1] POST /api/auth/webauthn/register-options
    السيرفر يولّد "تحدي" عشوائي (challenge)
    يُحفظ في الجلسة مؤقتاً
         ↓
[2] المتصفح يستدعي navigator.credentials.create()
    الجهاز يطلب من المستخدم: بصمة / وجه / PIN جهاز
         ↓
[3] POST /api/auth/webauthn/register-verify
    السيرفر يتحقق من التوقيع
    يُحفظ في WebAuthnCredentialModel:
    { credentialId, credentialPublicKey, counter, transports, deviceName }
مراحل تسجيل الدخول بالبصمة
[1] POST /api/auth/webauthn/auth-options
    السيرفر يولّد تحدياً جديداً
    إذا كان اسم المستخدم معروفاً → يُرسل قائمة بمعرّفاته المسجّلة
    إذا لم يعرف → "discoverable" (الجهاز يختار)
         ↓
[2] المتصفح يستدعي navigator.credentials.get()
    الجهاز يتحقق من هوية المستخدم
         ↓
[3] POST /api/auth/webauthn/auth-verify
    السيرفر يتحقق من التوقيع الرقمي
    يتحقق من counter (يمنع إعادة التشغيل)
    يسجّل الدخول ويُنشئ DeviceToken (صالح 14 يوم)
الـ counter — درع الحماية
// قبل التحقق: counter = 50
// بعده: counter = 51  (يُحدَّث دائماً)
// لو أحد نسخ البيانات وحاول إعادة استخدامها:
// counter = 50 < 51  → يرفضها السيرفر
Quick PIN (بديل لأجهزة بلا حساسات)
المستخدم يضبط PIN من 4-8 أرقام
يُحفظ مشفراً بـ bcrypt
تسجيل الدخول: POST /api/auth/quick-pin/verify
📱 ثالثاً: التأكيد عبر الهاتف (2FA + Phone Verify)
هذا نظامان مختلفان:

أ) التحقق الثنائي عند تسجيل الدخول (2FA)
4 طرق متاحة في نفس الوقت:

الطريقة	الآلية
TOTP (QiroxAuthenticator)	مفتاح سري مشترك + وقت → رمز 6 أرقام كل 15 ثانية
Email OTP	رمز 6 أرقام يُرسل للبريد عبر SMTP2GO
Push Approval	إشعار push → المستخدم يختار رقماً من 3 أرقام
Recovery Passphrase	جملة سرية احتياطية
المستخدم يدخل كلمة المرور
         ↓
السيرفر يتحقق → يحفظ { userId, pending2FA: true } في الجلسة
         ↓
يُرسل للمستخدم: "اختر طريقة التحقق"
         ↓
POST /api/auth/verify-2fa  { method: "totp", code: "123456" }
         ↓
توقيع الدخول الكامل
TOTP بالتفصيل:

السيرفر يولّد مفتاح base32 سري → يُحفظ في User.totpSecret
المستخدم يمسح QR بتطبيق المصادقة
التطبيق: HMAC(secret, floor(timestamp/15)) → رمز 6 أرقام
السيرفر يحسب نفس العملية ويقارن (نافذة ±3 دورات)
Push Approval بالتفصيل:

السيرفر يولّد تحدياً: رقم عشوائي مثلاً 47
يرسل push إشعار: "اختر الرقم الصحيح"
الفرونت يعرض 3 أرقام: [23, 47, 91]  (اثنان وهميان)
المستخدم يضغط 47
POST /api/auth/verify-2fa { method: "push", code: 47 }
السيرفر: 47 === challengeNumber ✓
النظام مبني على Web Push API — وهو المعيار العالمي الذي تستخدمه جميع المتصفحات لإرسال إشعارات حتى وإن كان التطبيق مغلقاً. إليك كيف يعمل بالتفصيل:

المكونات الأربعة الأساسية:

1. VAPID Keys (مفاتيح التشفير)
في server/push.ts — هي مفاتيح تشفير خاصة بالتطبيق تُثبت لمتصفح الجهاز أن الإشعار قادم من سيرفر QIROX المعتمد وليس من أي جهة أخرى.

2. Service Worker (client/public/sw.js)
هو ملف JavaScript يعمل في الخلفية على جهازك حتى بعد إغلاق المتصفح. عندما يصل إشعار Push، الـ Service Worker هو من يستيقظ ويعرض الإشعار على شاشة القفل — هذا هو سرّ ظهوره بدون فتح التطبيق.

3. قاعدة بيانات الاشتراكات (MongoDB)
كل جهاز يوافق على الإشعارات يُسجَّل endpoint خاص به في PushSubscriptionModel. هذا الـ endpoint هو عنوان البريد الرقمي للجهاز — السيرفر يرسل الإشعار لهذا العنوان، وGoogle/Apple يوصّلونه للجهاز.

4. server/push.ts — sendPushToUser()
عند إرسال إشعار، يستخرج السيرفر جميع أجهزة المستخدم من قاعدة البيانات ويرسل Push لكل واحد منها عبر مكتبة web-push.

مسار الإشعار من الكود للشاشة:

سيرفر QIROX
    ↓ sendPushToUser()
خوادم Google (FCM) أو Apple (APNs)
    ↓
متصفح الجهاز / Safari على iPhone
    ↓
Service Worker يستيقظ
    ↓
showNotification() → شاشة القفل ✅
ملاحظة مهمة للـ iPhone:
يعمل فقط على iOS 16.4+ مع إضافة الموقع لـ Home Screen (Add to Home Screen). هذا قيد من Apple وليس من الكود — قبل iOS 16.4 كانت Apple لا تدعم Web Push أصلاً.

